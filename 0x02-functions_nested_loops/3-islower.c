#include "holberton.c"

/**
 * _islower - checks for lowercase character
 *
 * @c: parameter hard-coded in main function
 *
 * Return: 0 or 1
 */

int _islower(int c)
{
return (c >= 97 && c <= 122);
}
